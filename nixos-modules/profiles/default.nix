# Profile modules aggregator
# Profiles bundle multiple features together
# Will be populated in Phase 3
{
  imports = [
    # Desktop profile (will add in Phase 3)
    # ./desktop.nix

    # Development profile (will add in Phase 3)
    # ./development.nix

    # Server profile (will add in Phase 3)
    # ./server.nix
  ];
}
