# Development Features Aggregator
#
# Imports development environment configurations including build tools,
# virtualization (Docker/Podman), and environment management (direnv).
{ ... }:
{
  imports = [
    ./base.nix
  ];
}
