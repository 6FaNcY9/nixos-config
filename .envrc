#!/usr/bin/env bash
# Automatically load the Nix devshell for this project
# This activates the default devshell when entering this directory
# and deactivates it when leaving.
#
# First time setup:
#   1. Install direnv: Add to home-manager or system packages
#   2. Add hook to shell: direnv hook fish | source (or bash)
#   3. Approve this file: direnv allow
#
# See: https://direnv.net/ for more information

use flake

# Optional: Load local overrides if they exist
# Create .envrc.local for machine-specific settings (gitignored)
if [ -f .envrc.local ]; then
	source_env .envrc.local
fi
