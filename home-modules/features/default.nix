# Home feature modules aggregator
{
  imports = [
    # Desktop features (will add during migration)
    # ./desktop

    # Editor features (will add during migration)
    # ./editor

    # Shell features (will add during migration)
    # ./shell

    # Terminal features (will add during migration)
    # ./terminal
  ];
}
