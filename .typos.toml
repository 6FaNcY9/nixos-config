[files]
# Exclude SOPS-encrypted secrets — base64-encoded ciphertext triggers false positives
extend-exclude = ["secrets/**"]

[default.extend-words]
# nftables keyword for output interface
oif = "oif"
